/**
 * 图表更新：在actions执行之后进行更新
 * 1. 更新数据，每次action之后的数据不同
 * 2. 组件更新：更新非渲染逻辑，eg：比例尺映射等等
 * 3. 视图更新
 *
 * Chart update: Update after actions are executed
 * 1. Update data, the data after each action is different
 * 2. Component update: update non-rendering logic, e.g. scale mapping, etc.
 * 3. View Update
 */
